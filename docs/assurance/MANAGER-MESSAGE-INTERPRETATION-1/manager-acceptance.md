# Manager message interpretation — acceptance

Accepted 2026-09-18 local, PROTOTYPE, under the human's explicit “Manager interprets messages; runtime constructs records” request. Source remains uncommitted. This is manager-recorded acceptance after independent review, not the held Stage-4 runtime acceptance gate.

## Delivered and verified

| Obligation | Delivered evidence |
|---|---|
| MMI-L01 | Completed off-shape output is retained and becomes pending, including alternate prose and artifact cardinality/type mismatches. It does not become a code-rework cycle. |
| MMI-L02 | The apply command accepts semantic content; existing constructors/validators supply and check runtime-owned envelopes/identities. All four result paths are covered with no provider call on application. |
| MMI-L03 | Incomplete meaning remains pending. Read-only same-role clarification preserves identity and iteration, including fresh conversations for old no-ID slices and refusal rather than silent fallback for existing unavailable/mismatched bindings. Failure/question records are retained. |
| MMI-L04 | Original output is not rewritten. Separate role-bound audit records and tracked report/limitations text reveal interpretation and its source. Applicable input/candidate/raw/run checks remain. Interpretation is not independent execution evidence. |
| MMI-L05 | Restart preserves the pending step. Evidence/review/requirements/legacy routing is exercised, including both roles in one iteration and exact-command retry after a recoverable consume-write failure. |
| MMI-L06 | Existing use case, CLI and two operating documents updated; pinned shared/role prompt assets unchanged. Real retained-prose example exercised through the built CLI. |
| MMI-P01/P02 | Existing admission, assurance/evidence coverage, create-only publication, human authority, role/session separation and permissions remain. The original 183 tests plus new cases pass. |

## Review and actual execution

Sol built and Terra independently reviewed through the target-owned relay. The second round reused the original, distinct builder/reviewer native sessions; see native-relay-continuation.json. Terra's first review requested preservation and session-refusal corrections; its second approved 206-test candidate is preserved, not replaced.

Manager acceptance was withheld after a public-use-case counterexample showed builder and reviewer interpretations in one iteration colliding on the audit filename. The relay had already captured its packet before the manager added this finding; a later file update was not assumed to reach running actors. Explicit SAME-session Sol/Terra follow-ups corrected/reviewed that composition bug, analogous clarification record naming, exact-command retry, attribution wording, and fresh legacy reviewer build-report delivery. Final independent review: approved (implementation-review.md). The manager's original counterexample now reaches done with no repeat provider call.

Manager independently executed **typecheck, all 209 tests, fresh build, and diff whitespace check**, all passing. The suite is hermetic with fake providers and isolated HOME/PATH. Main working-tree tests did not invoke installed providers. The separate live evidence below did.

Live demonstration: one actual read-only Terra review in an owned temporary Git target, already at review-impl with existing work and no session IDs. It returned clear prose without STATUS/JSON. The runtime retained it as pending and created the reviewer session binding. The manager read both the output and fixture README, submitted an explicit interpretation, and the built CLI reached done at the same iteration. Original build/report/run bytes remained identical; a sentinel provider executable was not invoked during apply. See live-retained-output-evidence.json. This proves legacy live transport/application, not live v2/v3 assurance on another product; those paths have headless test evidence.

## Under the hood and minimality

No new production module, dependency, daemon, interpretation model, session registry or durable schema family was added. The existing relay owns the pending state, resumes its existing consume/routing steps, and uses existing assurance validators. Generated per-dispatch guidance permits prose while leaving old pinned prompt-file bytes intact. A manager still judges meaning and whether uncertainty is resolved; code checks record integrity, not whether the manager's judgment is true.

- PendingInterpretation/ManagerOperation sums — users: current relay and CLI; variation: four current result contracts and apply/clarify operations; rejected nullable booleans or rerunning a provider for serialization.
- Local shared routing functions — users: normal and manager-interpreted result paths; variation: direct versus interpreted delivery; rejected duplicated publication/routing policy.
- clarificationRunner callback — users: relay, composition root and tests; variation: the actual recorded Codex/Claude/Copilot provider versus current command defaults; rejected calling the wrong currently configured runner.
- CompletedProviderOutput capture — users: build, review and clarification retention; variation: single text versus the provider port's artifact set; rejected dropping all but artifact zero.
- Local clarification outcome/audit operations — users: success, refused dispatch, thrown and returned failures, and four interpretation routes; variation: actual execution outcomes and exact retry; rejected duplicated retention logic and unconditional create-only retry collisions. No transaction framework was introduced.

## Bounds, compatibility and handoff

- Missing IDs on old unfinished slices start new conversations with current inputs, NOT a new slice or discarded work. Native history is a convenience, never the source of requirements/evidence. Actual cache hits are neither measured nor guaranteed.
- A present unusable ID is not silently erased. Investigate and consciously recover with the user; a fresh fallback is not implied.
- Legacy mode remains legacy: it does not gain Stage-3 candidate identity guarantees. Reviewed modes retain their applicable admission/candidate checks.
- Existing partial durable-publication failures still use the existing blocked/manual recovery path; this increment does not provide generic transactional publication or Stage-4 recovery. Exact pending-command retry covers the demonstrated recoverable consume-write failure without overwriting its audit.
- Initial native ID persistence still depends on receiving provider process output; no crash-time persistence guarantee before first observation is claimed.
- Lint was not run: ESLint is absent and installation was not authorized. Mandatory typecheck/tests/build passed.
- ASSURANCE-4/5 remain held. No other project's interrupted slice was automatically resumed or modified. No main-repository commit, push or deployment was performed.

Administrative closeout updates slice status/roadmap only. Exact reviewed scope bytes are retained as reviewed-scope.md (matching the reviewed hash in input-baseline.md); accepted source identities are listed separately. Historical reviews/baselines were not rewritten. Use docs/MANAGER.md and docs/contracts/target-owned-relay.md for commands and record semantics.
