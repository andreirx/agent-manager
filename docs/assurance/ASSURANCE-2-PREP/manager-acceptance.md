# ASSURANCE-2 preparation — manager acceptance

Status: ACCEPTED design and implementation inputs, 2026-09-12. Runtime implementation pending.

Authority: [approved rollout](../ASSURANCE-0/human-authorization.md), standing self-build operator closeout. Builder Codex/gpt-5.6-sol/high; separate reviewer Codex/gpt-5.6-terra/high, same provider. [Review 0](review-0.json) approved the first draft; manager separately found two executable-bootstrap defects in [checkpoint](manager-checkpoint.md). [Review 1](review-1.json) approves their correction and the whole two-document candidate. No reviewer verdict was rewritten.

The accepted contract and packet allocate 16 L obligations across five unchanged H files. Their exact identities and actual four author/review invocations are in [preparation evidence](preparation-evidence.json). All 122 starting tracked paths were byte-identical; only two allocated draft documents changed. Document whitespace/JSON checks passed. Runtime tests are not claimed for a document-only pass.

M-A2-PREP-01: the document posture now separates existing hash-bound ADMISSION_ALLOCATION from the authored SLICE_DOC and candidate REVIEW_BASELINE. Both the accepted input allocation and newly produced output allocation are checked at their respective boundaries; legacy SPEC discovery stays unchanged. This is a current document-authoring distinction, not an admission bypass.

M-A2-PREP-02: the new implementation manifest fixes exactly 17 requirement/dependency inputs and excludes all 14 runtime outputs. Old role prompts come from the accepted 47e647c runner snapshot; edited target prompts are outputs, not mutable authorities for the ongoing run. Only a fresh reviewed candidate runner supplies new prompt bytes for the subsequent demonstration. Existing INPUT-1/2/3 remain historical and unchanged.

The manifest and manual review/approval were assembled by the manager after substantive review. The five unchanged H files reuse [ASSURANCE-0 acceptance](../ASSURANCE-0/manager-acceptance.md); review1 checks the new allocation/design and its two corrections. This does not claim the reviewer inspected a not-yet-written manifest, cryptographic authentication, or new per-ID machine enforcement. [A2-C00 predecessor probes](admission-probes.json) passed: the new chain admits, edits to all 14 output copies still admit, and mutation of VISION refuses with digest-mismatch. Every dry-run left its disposable target byte-identical; the disposable copy was removed. The runner was built from the accepted 47e647c snapshot without installing dependencies.

The authoring-time PROPOSED/PREPARED labels remain frozen; this external exact-revision record supplies acceptance, avoiding a post-review input edit. This accepts no runtime implementation and no ASSURANCE-3–5 gate.

Architecture judgment: the eight earned structures are enumerated in the two accepted documents with current users, concrete variation and rejected alternatives. They extend existing policy/ports/adapters rather than adding modules or dependencies. The additional ADMISSION_ALLOCATION packet field serves the real requirements-authoring bridge; reusing SLICE_DOC would conflate immutable authority with mutable output. The implementation stays one vertical increment because structured review needs actual delivered content, and delivery is exercised by the real next-stage review. Bounded checkpoints remain mandatory if concrete edit distribution challenges that size. This advances VISION's file-inspectable role transport without making the relay an authority or semantic proof engine.
