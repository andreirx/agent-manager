# ASSURANCE-0 — manager acceptance

Status: ACCEPTED for dependent ASSURANCE-1 implementation, 2026-09-11.
Authority: [human authorization](human-authorization.md); standing self-build operator closeout.

Manual bootstrap only. Builder: Codex CLI gpt-5.6-sol/high. Independent reviewer: separate Codex CLI gpt-5.6-terra/high invocation, same provider. Four cycles total with a manager checkpoint at the three-cycle cap. [Final review](review-3.json) approves requirement/design quality for all 9 H / 56 L entries, not software implementation. [Manager checks](manager-checks.json) separately record source preservation, actual run provenance and limits.

Review closed: exact manifest field count; requirement-derived source overlap and repeatable classification roles; allocation-checked dry-run rather than premature admission claim; unused core barrel removal; declared run IDs distinguished from observed separate invocations. Prior reviews are retained alongside this record. No new product decision or approved requirement wording changed.

The operator accepts the record contract and bounded ASSURANCE-1 packet under the approved design refinement. Their authoring-time PROPOSED/PREPARED labels do not self-authorize them; this external acceptance and the exact baseline chain do. Requirement files retain reviewed bytes, including original pending-review labels; current review status lives here and in the catalog. No software requirement is claimed satisfied yet.

The input manifest is assembled by the manager AFTER review from the unchanged reviewed input bytes; the manager binds the copied review report to that closure. This does not claim the model read a manifest file that did not yet exist. No hashing machinery proves semantic quality or identity authenticity.

No new code abstraction was introduced in bootstrap. Earned record concepts: same-file requirement metadata (admission + authoring; source/identity variation; rejected duplicate requirement database); manifest/review/approval chain (admission + manager; exact accepted revisions; rejected editable status authority); persisted complete mode (dispatch + resume; legacy/admission distinction; rejected folder detection). These remain stage1 implementation work.

Publication correction: the first staged whitespace gate refused four extra EOF blank lines. No commit occurred. The manager removed only those redundant newlines, asserted trimEnd equality, and finalized the unpublished baseline hashes. [Exact prior candidate and delta](publication-correction.json) retain the original bytes; semantic review evidence is reused, not falsely labelled a new reviewer execution. The manifest/approval become the first published baseline only after the repeated gate passes.
