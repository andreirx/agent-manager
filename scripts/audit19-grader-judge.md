Independent second-opinion review of repo-graph v0.19.0's output usefulness for coding agents, SEVENTH round. No rubric — judge "as the agent receiving this output: does it help, and could it mislead me?"

READ: the supplemental captures at /private/tmp/audit19/captures/ (57 files; prefixes name the slice they re-check: tme- trust module edges, as- alias suspicion, cbr- call-binding receiver, cir- C++ include roots, ech- explain cycles honest, dep- deps ecosystem partition, ets- explain type sections, cs- complexity scope, dd- docs discovery, psb- python self binding, sd- seed document, ho- the find→explain→callers hand-off) and, for the broad view, the smoke captures under "<SMOKE_RUN>" (per-repo <repo>-<cmd>.txt for trust, modules list, check on every corpus).

CONTEXT: the v0.18.0 round (docs/audits/2026-09-08-per-command-usefulness-v0.18.0.md) found the fabrication class had RETREATED to the symbol layer — receiver-bearing calls looping back onto the enclosing class, `explain` inventing import-cycle arrows from a sorted member set, `explain <Type>` rendering nothing, trust's Suspicious Modules structurally zero on Cargo/Gradle workspaces, docs discovery blind to src/site and README.rst. Nine slices plus two follow-ups shipped against those root causes (RC-1 … RC-11); docs/ROADMAP.md's SHIPPED blocks (2026-09-13 onward) record each expected outcome with file:line witnesses.

DELIVER: per-capture verdicts (helped / neutral / would-mislead, one line why, with the file:line in the analyzed repository you checked); any output that would actively MISLEAD an agent (quote + ground truth + your search); whether the symbol-layer fabrication class is dead in the field; whether a NEW class has appeared (name it from the captures, not from theory); the three outputs an agent would most want fixed next, each tied to a filed follow-up name if one exists (CALL-BINDING-RECEIVER-2, CYCLES-WALK-DETERMINISM-1, TS-ALIAS-RESOLUTION-1, PYTHON-ATTRIBUTE-CALLS-1, CONCERN-HINTS-1, CPP-INCLUDE-SUFFIX-1, DOWNGRADE-LABELS-1, SEED-LOGTURN-1, SEED-CPP-CLASS-DOC-1, DOC-RELEVANCE-STEMS-1, EXPLAIN-BASES-1) or named as new.
RULES: rmap is READ-ONLY for you — export RMAP_STATE_ROOT="<RETAINED_ROOT>" RMAP_SOCKET_PATH="<RETAINED_ROOT>/daemon.sock" RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off for any extra probe (binary /Users/apple/.local/bin/rmap, v0.19.0); never the operator's real state root; never index/refresh; never `pkill`. Verify from source in the checkouts (/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/, /Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG). Tool output is a claim.
