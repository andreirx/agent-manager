Per-command usefulness audit of repo-graph v0.18.0 (the rmap code-intelligence CLI), SIXTH round. You produce the grade evidence for the house-format report.

EVIDENCE:
1. Smoke captures: the newest directory under "/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/smoke-runs/" (per-repo per-command outputs <repo>-<cmd>.txt; linux intentionally skipped this round — its manifest entry must now say so).
2. Supplemental captures: /private/tmp/audit18/captures/ — ~45 probes re-checking every slice shipped since v0.17.0, grouped by prefix (ir- import resolution, mm- modules method, cpp- declarators, si- symbol identity, dc- deps classifier, ht- headline truth, sc3- seeds, a5- audit5 minors, ec- exit codes, dr- daemon residuals).
3. BASELINES (in the repo): docs/audits/2026-09-06-per-command-usefulness-v0.17.0.md (last round's grades, its "class relocated to the command-summary layer" verdict and defects D1–D15), docs/audits/2026-09-06-root-causes-v0.17.0.md (the code-level root cause of every D — grade whether each cause is FIXED at the cause, not papered), docs/audits/2026-09-06-codegraph-vs-rmap.md (the hand-off defects H1–H6), docs/audits/2026-09-06-change-path-characterisation.md.

WHAT SHIPPED since v0.17.0 (grade whether each delivers IN THE FIELD, on the captures): IMPORT-RESOLUTION-RUST-1 (cross-crate `use` resolves; modules list/cycles zero-states carry resolved+unresolved counts); IMPORT-RESOLUTION-JAVA-1 (FQN → file; wildcard/ambiguous counted; grpc-java projectDir); DAEMON-RESIDUALS-2 (FK-child indexes; `rmap repo rebuild` verb with a sentinel; doctor prunable share / last-pass duration / budget overrun); CPP-DECLARATORS-1 (macro-wrapped function names; forward decls as (decl) below definitions; C++ inheritance edges resolve; type beats its constructors with a pointer); SYMBOL-IDENTITY-1 (what find prints, explain/callers accept — qualified suffix; a miss is never Confidence: high); DEPS-CLASSIFIER-1/1B (asgiref used; per-row basis; type-only; every ecosystem named); HEADLINE-TRUTH-1 (one file universe stated; complexity headline names the symbol; --full marker truthful; surfaces order + no "0 project surfaces"; inferences [test] + anchors; type-only verdict survives test-only partitions); MODULES-METHOD-1 (method line + repo-specific docs recommendation on modules/orient — human ruling: no more module discovery); SEED-CHUNK-3 (field tier; [field]; write-path methods still below the 0.30 floor by design — SEED-DOCUMENT-1 filed); AUDIT5-MINORS-1 (docs kinds name/dir-only; boundaries line sets; not-found cursor header; LiveGraph posture lines gone from trust; doctor [note]; smoke skipped accounting; Express provider lines); EXIT-CODES-1 (docs/contracts/exit-codes.md; dead exits 4 with stdout verdict; verdict commands' codes documented).

DELIVER, in this order: (1) the GRADE MATRIX — command × repo class (RUST/TS-internal, JAVA, C/C++, PYTHON, TS/JS-legacy) × HIT/EVIDENCE/HONESTY/ECONOMY, letter grades, same rows as last round; (2) a SHIPPED-FIX VERDICT table (FIXED / PARTIAL / REGRESSED / DORMANT per slice, with the capture citation and the ground-truth search you ran); (3) NEW DEFECTS ranked by product impact, each with the capture quote + ground truth + your exact search (when you claim something does NOT exist, state every extension incl. .mts/.cts/.mjs and script/config dirs); (4) dimension deltas vs v0.17.0 and vs the codegraph head-to-head where the same probe exists; (5) keep-and-imitate; (6) a one-paragraph verdict answering: is the "absence stated as a finding / command-summary fabrication" class dead in the field, and what is the new dominant class?
RULES: rmap is READ-ONLY for you — export RMAP_STATE_ROOT/RMAP_SOCKET_PATH to the retained audit-v0.18.0 root (given in the task) for any extra probe; never the operator's real state root; never index/refresh. Tool output is a claim — verify from source. Directionally correct beats pedantic: a defect is only a defect if a user asking about a repo is misled or unhelped.
