Per-command usefulness audit of repo-graph v0.19.0 (the rmap code-intelligence CLI), SEVENTH round. You produce the grade evidence for the house-format report.

EVIDENCE:
1. Smoke captures: "<SMOKE_RUN>" (per-repo per-command outputs <repo>-<cmd>.txt; linux intentionally skipped this round — its manifest entry says so).
2. Supplemental captures: /private/tmp/audit19/captures/ — 57 probes re-checking every slice shipped since v0.18.0, grouped by prefix (tme- trust module edges, as- alias suspicion, cbr- call-binding receiver, cir- C++ include roots, ech- explain cycles honest, dep- deps ecosystem partition, ets- explain type sections, cs- complexity scope, dd- docs discovery, psb- python self binding, sd- seed document, ho- the find→explain→callers hand-off family).
3. BASELINES (in the repo): docs/audits/2026-09-08-per-command-usefulness-v0.18.0.md (last round's grades, the "fabrication class retreated to the symbol layer" verdict, defects D-*, the queue Q1–Q9), docs/audits/2026-09-08-root-causes-v0.18.0.md (RC-1 … RC-11 — grade whether each cause is FIXED at the cause, not papered), docs/requirements/README.md and docs/requirements/rg-req-*.md (the requirement lines each slice implements — the verdict is per L, not per symptom), docs/ROADMAP.md ship blocks (search "SHIPPED" from 2026-09-13 on) which record each slice's expected AFTER literals with file:line witnesses.

WHAT SHIPPED since v0.18.0 (grade whether each delivers IN THE FIELD, on the captures; the ROADMAP block names the exact expected literal): TRUST-MODULE-EDGES-1 (Suspicious Modules from the derived module edge set — repo-graph 48 → 6 genuinely isolated; fans == modules deps); CALL-BINDING-RECEIVER-1 (an indirect receiver never binds to the enclosing class without type evidence — the fabricated self-row on leveldb::DBImpl::Recover gone; receiver self-loops leveldb 155 → 0, vcmi 836 → 36, duckdb 2522 → 53); CPP-INCLUDE-ROOTS-1 (module-local include roots — poco 0 → 79 module pairs, `Net → Foundation (616 file-level imports)`, a real Foundation cycle; ambiguous includes counted never bound); EXPLAIN-CYCLES-HONEST-1 (explain's "Import cycles" prints the ring `cycles` prints, never an alphabetical chain; large SCCs `members (unordered)`); DEPS-ECOSYSTEM-PARTITION-1 (per-ecosystem views; django `deps list --ecosystem npm` no longer says `undeclared 102` for Python imports; foreign-file references named); EXPLAIN-TYPE-SECTIONS-1 inc 1 (explain <Type> renders Members / Referenced by; `Callers (0) — a type is not called; see Members / Referenced by`); COMPLEXITY-SCOPE-1 (generated/vendored/test symbols excluded from complexity centers with the count stated — poco's pcre2 `match (cx 1580)` gone, `Windows1251Encoding.cpp — convert (cx 345)` first; `--include-all`); DOCS-DISCOVERY-1 (one stem list × one extension list × one doc-tree rule incl. src/site — hadoop docs 22 → 556 and `read: README.txt`; django `read: CONTRIBUTING.rst, README.rst, docs/`; the refused markup counted `+N markdown/prose files outside a docs tree not scanned (--json for the rule)`); PYTHON-SELF-BINDING-1 (self.<m> binds through the MRO — django `BaseHandler.get_response` Callers (0) → (2); ambiguous collisions stay unresolved with `self_call_ambiguous_mro`); ALIAS-SUSPICION-1 (the alias downgrade names a module and its count or disappears — kafka loses the false reason, FRAKTAG `packages/ui (55 …)`); SEED-DOCUMENT-1 (a METHOD chunk's seed document carries its class's first sentence — FRAKTAG `find "where are conversations persisted to disk"` now shows `ConversationManager.createSession` at 0.333 above the 0.30 floor; leveldb byte-identical).

DELIVER, in this order: (1) the GRADE MATRIX — command × repo class (RUST/TS-internal, JAVA, C/C++, PYTHON, TS/JS-legacy) × HIT/EVIDENCE/HONESTY/ECONOMY, letter grades, the same rows as last round; (2) a SHIPPED-FIX VERDICT table (FIXED / PARTIAL / REGRESSED / DORMANT per slice, with the capture citation, the ROADMAP literal it was checked against, and the ground-truth search you ran in the checkout); (3) NEW DEFECTS ranked by product impact, each with the capture quote + ground truth + your exact search (when you claim something does NOT exist, state every extension incl. .mts/.cts/.mjs and script/config dirs); (4) dimension deltas vs v0.18.0; (5) keep-and-imitate; (6) a one-paragraph verdict answering: is the symbol-layer fabrication class (round six's dominant class: receiver self-loops, invented cycle arrows, empty explain on types) dead in the field, what is the new dominant class, and which of the filed follow-ups (CALL-BINDING-RECEIVER-2, CYCLES-WALK-DETERMINISM-1, TS-ALIAS-RESOLUTION-1, PYTHON-ATTRIBUTE-CALLS-1, CONCERN-HINTS-1, CPP-INCLUDE-SUFFIX-1, DOWNGRADE-LABELS-1, SEED-LOGTURN-1, SEED-CPP-CLASS-DOC-1, DOC-RELEVANCE-STEMS-1, EXPLAIN-BASES-1) the captures show to matter most for a user asking about a repo.
RULES: rmap is READ-ONLY for you — export RMAP_STATE_ROOT="<RETAINED_ROOT>" RMAP_SOCKET_PATH="<RETAINED_ROOT>/daemon.sock" RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off for any extra probe (the binary is /Users/apple/.local/bin/rmap, v0.19.0); never the operator's real state root; never index/refresh; never `pkill` anything. Tool output is a claim — verify from source in the checkouts under /Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/ and /Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG. Directionally correct beats pedantic: a defect is only a defect if a user asking about a repo is misled or unhelped. Every claim you make carries a capture path and a file:line from the analyzed repository.
